# Required metadata
sonar.projectKey=rennes-metropole.tabou2
sonar.projectName=Tabou2 Front
sonar.projectVersion=1.0.0

# Comma-separated paths to directories with sources (required)
sonar.sources=assets,configs,js

# Language
sonar.language=ts

# Encoding of the source files
sonar.sourceEncoding=UTF-8

sonar.exclusions>=**/node_modules/**/*,**/*.spec.ts,**/*.e2e.ts,**/environments/**/*,**/*.routes.ts,**/main.ts, **/test-setup.ts, **/polyfills.ts
sonar.coverage.exclusions=**/*.ts