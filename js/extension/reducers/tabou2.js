import {
    TEAR_DOWN
} from '../actions/tabou2';

export default function tabou2(state = {enabled: false}, action) {
   const type = action.type;
   switch (type) {
        default:
            return state;
    }
}